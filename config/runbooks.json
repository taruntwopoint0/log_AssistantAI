{
  "RB-GATEWAY-60S": {
    "title": "Intermediary cuts large responses at a fixed ceiling",
    "symptom": "Large batches fail at a consistent round-number elapsed time while small batches on the same endpoint succeed all day. The exception is a premature end of response, not a connect failure.",
    "checks": [
      "Compare elapsed time across failures. A cluster within a few seconds of a round number is a configured ceiling, not congestion.",
      "Confirm small batches to the same host still return 200 in the same window.",
      "Ask the gateway team for the read/response timeout on the route, not just the connect timeout.",
      "Check for a proxy, WAF or load balancer between the worker and the host that nobody owns end to end."
    ],
    "fix": "Chunk the request into batches that complete inside the ceiling. At the observed rate, 200 records finishes in roughly 36 seconds, leaving headroom under a 60 second cutoff. Chunking is inside our control and testable today; raising the gateway timeout needs another team and a change window.",
    "do_not": [
      "Do not raise the HttpClient timeout. The client is not the component cutting the connection.",
      "Do not retry the same oversized batch. It will fail at the same ceiling every time and multiply load.",
      "Do not escalate to the Galileo platform team. Galileo is answering small batches normally."
    ],
    "escalate_to": "Network / API Gateway team, with the elapsed-time table attached"
  },

  "RB-GALILEO-DOWN": {
    "title": "Galileo platform unavailable",
    "symptom": "All calls to Galileo fail regardless of batch size, typically with 502, 503 or a connect-level failure.",
    "checks": [
      "Confirm failures are size independent. If small batches also fail, the platform is genuinely down.",
      "Check the Galileo status page and any active platform incident.",
      "Confirm the failure window matches a Galileo maintenance slot."
    ],
    "fix": "Hold the sync and wait for platform recovery. Once Galileo returns, re-run the sync for the missed window.",
    "do_not": [
      "Do not chunk the payload. Batch size is not the variable when every size fails.",
      "Do not restart the worker service. It will fail identically on the next cycle."
    ],
    "escalate_to": "Galileo Platform team"
  },

  "RB-NETWORK": {
    "title": "Network path failure to a dependency",
    "symptom": "Connection refused, no such host, or connect timeout. The failure happens before any response bytes arrive.",
    "checks": [
      "Resolve the hostname from the worker host itself, not from a workstation.",
      "Test TCP reachability on the target port.",
      "Check whether a firewall rule or proxy configuration changed recently.",
      "Confirm the worker host has not lost its route or DNS server."
    ],
    "fix": "Restore the network path. A connect-level failure means the request never reached the application, so no application-side change will help.",
    "do_not": [
      "Do not chunk the payload. The request is not being sent at all.",
      "Do not investigate application code paths until the host resolves and connects."
    ],
    "escalate_to": "Infrastructure / Network team"
  },

  "RB-SERVICE-CRASH": {
    "title": "Worker service stopped or crash-looping",
    "symptom": "The service is not running, or the log ends abruptly with an unhandled exception and a restart.",
    "checks": [
      "Get-Service for the worker and its current state.",
      "Windows Event Log, Application channel, around the stop time.",
      "Check whether the service account password expired or the account was locked.",
      "Confirm the host did not reboot for patching."
    ],
    "fix": "Restart the service once and watch whether it stays up through a full cycle. If it crash-loops, capture the unhandled exception before restarting again.",
    "do_not": [
      "Do not restart repeatedly without capturing the exception. Each restart discards the evidence.",
      "Do not assume a downstream problem until the worker survives one clean cycle."
    ],
    "escalate_to": "ALD Automotive Integration"
  },

  "RB-ALDAVAR-DB": {
    "title": "Aldavar database unreachable",
    "symptom": "SQL exception on connect, login failure, or connection pool exhaustion before any records are fetched.",
    "checks": [
      "Confirm the SQL instance is reachable from the worker host.",
      "Check the service account login and whether it is disabled or locked.",
      "Look for connection pool exhaustion, which reads as a timeout rather than a refusal.",
      "Check for a blocking chain holding the source tables."
    ],
    "fix": "Restore database connectivity. If the pool is exhausted, find the leaked connections rather than raising the pool size.",
    "do_not": [
      "Do not raise Max Pool Size as a first move. It hides a leak rather than fixing it.",
      "Do not investigate Galileo. The sync never got as far as calling it."
    ],
    "escalate_to": "Database Services"
  },

  "RB-DB-VIEWS": {
    "title": "Source view empty or stale",
    "symptom": "The sync completes without error but fetches zero records, or a count far below the expected range.",
    "checks": [
      "Query the source view directly and compare the count to recent days.",
      "Check whether an upstream feed that populates the view has failed.",
      "Confirm the view definition was not changed recently."
    ],
    "fix": "Repair the upstream feed that populates the view. A zero-record sync is a silent failure and will not raise an error on its own.",
    "do_not": [
      "Do not treat a clean exit as success. Zero records is a failure that did not throw.",
      "Do not re-run the sync until the view is repopulated."
    ],
    "escalate_to": "Database Services"
  },

  "RB-AUTH-401": {
    "title": "Credential or token rejection",
    "symptom": "401 or 403 from the endpoint, usually immediately and regardless of batch size.",
    "checks": [
      "Check whether the client secret or certificate expired.",
      "Confirm the token audience and scope match what the endpoint expects.",
      "Check whether the endpoint changed its auth requirements recently."
    ],
    "fix": "Rotate or renew the credential. An immediate rejection at any batch size is an identity problem, not a capacity problem.",
    "do_not": [
      "Do not retry with backoff. A rejected credential does not become valid on retry.",
      "Do not chunk the payload. Size is irrelevant when the request is refused at the door."
    ],
    "escalate_to": "ALD Automotive Integration"
  },

  "RB-HEALTHY": {
    "title": "No failure detected",
    "symptom": "The run completed and every batch returned successfully.",
    "checks": [
      "Confirm the record count is in the expected range rather than zero.",
      "Confirm the run finished inside its normal window."
    ],
    "fix": "No action required.",
    "do_not": [
      "Do not treat a healthy run as proof that a recent change is safe. One clean cycle is one data point."
    ],
    "escalate_to": null
  },

  "RB-UNKNOWN": {
    "title": "Not recognised",
    "symptom": "The evidence in this log does not match any rule currently configured.",
    "checks": [
      "Read the raw log block directly. The tool has no opinion to offer here.",
      "Run the manual seven-source checklist.",
      "Once the true cause is known, add a rule to config/rules.json so the next occurrence is recognised."
    ],
    "fix": "No fix can be proposed. Declining to guess is the correct outcome when the evidence matches nothing known.",
    "do_not": [
      "Do not act on the closest-looking runbook. A near miss on a diagnosis is worse than no diagnosis."
    ],
    "escalate_to": "ALD Automotive Integration"
  }
}
